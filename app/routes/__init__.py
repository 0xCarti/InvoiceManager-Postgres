"""Flask blueprint package for InvoiceManager routes.

Blueprints are defined in the sibling modules (e.g., ``invoice_routes``) and
registered in :mod:`app.__init__`. Refer to ``docs/routes.md`` for a blueprint
to URL prefix mapping, notable dependencies, and shared behaviors.
"""
